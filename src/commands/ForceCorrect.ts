import { RedditBot } from "../RedditBot";
import { Comment } from "snoowrap";

export default async (bot: RedditBot, comment: Comment) => {
  // Check reported comment was by submitter
  // Check reported comment was a reply to comment by not submitter
  // Call WinProcessor
}